#include<bits/stdc++.h>
using namespace std;
//bitset<10000000001> T;
int main(void)
{
	/*
	for(int A=1; A<100; A++){
		for(int P=2; P<=32; P++){
			unsigned int F = pow(A, P);
			if(F) T[F] = 1;
		}
	}
	for(long long i=0; i<10000000000; i++)
		if(T[i]) cout << i << endl;
	*/
	//cout << pow(1000, 1/4.0) << endl;
	//int v = 4294967296;
	cout << INT_MIN << endl;
	return 0;
}
